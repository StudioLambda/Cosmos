name: Test Problem

on:
  push:
    branches: ["main"]
    paths:
      - .github/workflows/problem.yml
      - problem/**
  pull_request:
    branches: ["main"]
    paths:
      - .github/workflows/problem.yml
      - problem/**

jobs:
  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./problem
    steps:
      - uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.27.0-rc.2" # temp, remove when done.
          go-version-file: "problem/go.mod"
          cache-dependency-path: "problem/go.sum"

      - name: Download Dependencies
        run: go mod download

      - name: Test
        run: go test -race -v ./...

  format:
    name: Format
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./problem
    steps:
      - uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.27.0-rc.2" # temp, remove when done.
          go-version-file: "problem/go.mod"
          cache-dependency-path: "problem/go.sum"

      - name: Check formatting
        run: test -z "$(gofmt -l .)"

  vet:
    name: Vet
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./problem
    steps:
      - uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.27.0-rc.2" # temp, remove when done.
          go-version-file: "problem/go.mod"
          cache-dependency-path: "problem/go.sum"

      - name: Download Dependencies
        run: go mod download

      - name: Vet
        run: go vet ./...

  vuln:
    name: Vulnerability Scan
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./problem
    steps:
      - uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.27.0-rc.2" # temp, remove when done.
          go-version-file: "problem/go.mod"
          cache-dependency-path: "problem/go.sum"

      - name: Download Dependencies
        run: go mod download

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Vulnerability scan
        run: govulncheck ./...
