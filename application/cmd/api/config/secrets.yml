# Optional configuration secret source. When name is empty, no external secret
# is loaded. The named secret must contain a JSON object whose values overlay
# this configuration before the application services are constructed.
secrets:
  aws_secrets_manager:
    region: "${AWS_REGION}"
    endpoint: ""
    name: ""
