# problem/internal

Internal support code for the `problem` module.

## Scope

This package currently provides Accept-header parsing and quality negotiation
utilities used by the public `problem` package.

## Stability

Because this is an `internal` package, APIs are not exported for external
consumers and may change without notice.
